import { Meta, StoryObj } from '@storybook/react'
import {{pascalCase name}} from '.'

export default {
  title: '{{atomic}}/{{pascalCase name}}',
  component: {{pascalCase name}}
} as Meta

export const Default: StoryObj = {}
